# Decisions

## 2026-06-06 - Distill, do not copy, the 65-line guideline into global Codex rules

Decision: Add a compact global discipline section to `C:\Users\other\.codex\AGENTS.md` instead of pasting the full `CLAUDE.md`.

Rationale:

- The existing global Codex file already contains project onboarding, CodeGraph, testing, git, and safety rules.
- Copying the whole 65-line file would duplicate existing guidance and make future instruction conflicts more likely.
- The durable essence is four behaviors: clarify first, keep code simple, edit surgically, and verify with evidence.

